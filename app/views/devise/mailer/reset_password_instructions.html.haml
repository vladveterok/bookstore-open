%p= t('mailer.hello', email: @resource.email)
%p= t('mailer.reset_password.info')
%p= link_to t('mailer.reset_password.change_my_pass'), edit_password_url(@resource, reset_password_token: @token)
%p= t('mailer.reset_password.ignore_email')
